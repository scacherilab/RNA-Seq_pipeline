Dependencies:
1) The following Perl packages:
Pod::Usage

1) fastx_toolkit (tested on v0.0.13) (accessible through $PATH variable)
2) tophat, cufflinks, and cuffcompare must be installed and accessible through path listed in -path argument
3) merge.plx is included and must also be accessible through path listed in -path argument

